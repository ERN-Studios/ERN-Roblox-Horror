NOT a script — this is an object you insert in Studio.

Right-click the "Remotes" folder (under ReplicatedStorage)
→ Insert Object → RemoteEvent → rename to: Jumpscare

Used by: EntityKill (server) → JumpscareUI (client)
Name is case-sensitive — must be exactly "Jumpscare".
