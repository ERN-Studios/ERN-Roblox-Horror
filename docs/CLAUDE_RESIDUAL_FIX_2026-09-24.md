You are Claude Code working on BACKROOMS: STAY QUIET. Keep status updates short. The owner assigned heavy game code to Claude; Codex handles assets, independent review and publication QA. Work on branch `claude/trello-20260921` in `G:\Roblox\MongoTV`. Read `AGENTS.md`, `docs/CLAUDE_HANDOFF_2026-09-24.md`, `docs/CLAUDE_TOKEN_EARNER_REVIEW_2026-09-24.md`, `docs/CLAUDE_DEV_SUIT_VFX_REVIEW_2026-09-24.md` and the relevant live Studio source before writing. Studio is authoritative; do not overwrite concurrent edits. This CLI may lack direct Studio MCP: if so, prepare a precise repo patch against a verified matching live baseline and leave Studio application to Codex. Do not claim Studio parity from the repository alone.

Fix two bounded residual bugs:

1. Token Earner: `ZyntraMonetization` automatic Fuse/Lever and level-clear earnings call `tokenEarner.tier(player,60)` and ignore `known`, durably paying 1× on an ownership outage. A clear's CompletionId prevents replay. Implement a durable, bounded, idempotent pending-earned-token adjustment or an equally safe design. It must survive leave/rejoin and UpdateAsync lost-response retries, eventually settle after a definitive ownership snapshot, and never double-pay. Preserve base earned tokens and the gameplay completion atomically; include Friend Boost, Daily Clear and challenge rewards in clear earnings. **Future earned only**: a pass bought after an earn event must not retroactively multiply that event. Do not multiply existing balance, bought Token packs, gifts/refunds/admin grants. Update normalization/migrations and retry/backoff as needed. Replace the current completion-save case 15, which expects the faulty 1× fallback, with meaningful outage/rejoin/5×, duplicate-event, failed-write, lost-response, later-purchase and research tests. Keep all six passes off sale; do not publish or make real purchases.

2. Developer suit: visual fail-closed revocation exists, but a failed `Skins.SyncDeveloper` persistent write has no retry until next join/unrelated refresh. Add bounded idempotent durable cleanup retry, rechecking current DevAccess within the update transform. Cover failed-write/rejoin and two-viewer/forged equip in tests as feasible. Do not change cosmetic ownership for genuine developers.

Use small scoped commits, run relevant Luau offline suites and compile checks, and report source hashes and exactly which native validations remain. Do not touch Pool Slide animation IDs, Level 4/5, Trello, sale prices, product IDs, other assets, publication, or unrelated dirty files. Push plain, no force, only after inspecting exact staged diff. Update the existing PR #8 review description/handoff if possible. If Studio MCP is unavailable, stop before any claim that these fixes are game-saved or published; Codex will do the authoritative Studio application and further QA.
