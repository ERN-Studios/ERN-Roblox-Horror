# Level 5 QA — 2026-09-23

Status after Studio Play revision 2 and verified Roblox publication v2026. Actual Studio findings below were reported by the integration developer performing the play sessions. Offline checks are listed separately and do not establish multiplayer or device performance.

## Actual Studio checks

| Check | Result and evidence |
| --- | --- |
| Physical developer gate | LaverSneglen walked through the sealed Level 5 wall. The avatar had 17 gate-specific NoCollisionConstraints. |
| Disabled gate flag | Setting the developer flag off removed all constraints. Walking from x−24 toward the gate stopped at x−32.30 against the wall. Flag restored to true afterward. |
| Actual queue entry | One-player GUI queue station 18 started Level 5 during the first Play session. |
| Spawn and broad traversal | Healthy spawn and navigation through the whole map were observed. |
| Atrium stairs, revision 2 | Real character navigation succeeded on first and upper balconies, with route points around world x16955→16931 and z7→36→40→70. |
| House-stack stairs, revision 2 | Real character navigation succeeded on lower and upper stairs around x17048, z226→255→260→290. Character health remained 100. |
| Actual return control | Holding L for 2.5 seconds returned the character to the lobby: `InRound=false`, position approximately `(-1.4, 33.2, -859)`. |
| Round cleanup | After about six seconds, no Level 5 Workspace children remained; lobby stayed present, `SelectedLevel=1`, `RoundActive=false`. |
| Developer re-entry | `ServerStorage.Level5DevStart:Invoke(...)` returned true after cleanup. |
| Final console | Zero errors reported; only builder information and the lobby-return log. |
| Publication | Successful v2026 publish at 2026-09-23 21:05:06 UTC to existing place `131311258779917`, universe `10559217407`; current-session Studio log receipt retained. |

The initial local-return failure was traced to parking the lobby outside Workspace. It was corrected inside the Level 5 adapter, and the actual hold-L path passed after that correction. Earlier GUI automation uncertainty is therefore superseded by the successful revision 2 result.

Visual refinement included explicit embedded house volumes in the impossible stacks, stair/rail openings and enclosure across the different ceiling heights. Reference-inspired geometry is not an exact reconstruction of any supplied video frame.

## Offline checks performed

| Check | Result | Scope |
| --- | --- | --- |
| New runtime source compilation | Passed | Four runtime scripts compiled with official Luau 0.739; adapter compiled again after the lobby fix. |
| Shared integration compilation | Passed | All five proposed shared-script outputs compiled, including RoundUI. |
| Developer access and transport | 74 assertions passed | Actual routing module and extracted production authorization functions with mocked engine boundaries; independent level flags, allowlisted parties and transport behavior. |
| Round Entry Client | 59 assertions passed | Actual client source in a scheduler/engine harness, including valid Levels 4/5 and first invalid level 6. |
| Existing Level 3 continuation regression | Passed | Existing unchanged `test_no_level3_continue.py`; normal progression remains capped. |
| Studio/published lobby retention branches | 6 assertions passed | Actual extracted `parkLobby` function: keep the lobby in Studio, preserve published parking behavior. |

Evidence in the build folder: `integration/integration-manifest.json`, `runtime-compile-evidence.json`, `lobby-retention-check.json`, `test_level5_access.py`, `test_level5_lobby_retention.luau` and `integration/tools/tests/`. The first compile manifest predates the lobby fix; its adapter hash is superseded by the later lobby-retention evidence. The integration manifest is a proposal/compile record, not proof of the final installed source. A fresh final Studio export remains the authority.

## Preservation checks

- Initial authoritative export: `baseline/export.json`, 168 sources, no source/editor conflicts reported.
- Fresh pre-install shared-source reread was normalized as `baseline/latest-shared-export.json`.
- Final Studio export: `verification/export.json` and `verification/chunk-1.json` through `chunk-12.json`, 174 scripts, zero editor/source conflicts. The edit-place version reported 1999; this is not evidence of a new published version.
- Concurrent change was detected before applying GameManager edits; patches were regenerated without removing the other developer's queue, challenge-tracking or UI fixes.
- `artifacts/level5-20260923/studio-source-verification.json` records additional concurrent ZyntraChallenges/config/detector/monetization changes. Their presence is preservation evidence, not a Level 5 functional test of those features.
- Native pre-edit backup saved by the user: `output/level5-build/baselineOA.rbxlT.rbxl` (9,448,869 bytes, binary Roblox header verified), preserving the original filename. SHA-256: `1011e94bee1bb81d35f620ac11878239c1b9e67b881dee34f03653fa2c3b2f82`.
- No Level 4 source was part of the Level 5 edit set. All 12 Level 4 sources were verified identical to the initial authoritative Studio baseline after this work.

## Publication receipt

The current Studio session recorded `PublishSuccessful` at `2026-09-23T21:05:06.606Z`, publication confirmation at `.840Z`, eligible-player availability at `.860Z`, and `Add publish notes to v2026` at `.860Z`. Filtered evidence is saved under `artifacts/level5-20260923/publish-log-v2026.txt` and `publication-v2026.json`. It contains only the relevant publication lines, not the full session log. The previously reported edit-place value 1999 is superseded for publication status by this explicit v2026 receipt.

## Limits and final delivery checks

Not measured: physical mobile/tablet frame rate or memory, human multiplayer navigation, Mikkel's physical gate traversal, published reserved-server teleport/return, or behavior across live public servers. Both developer IDs are covered by the authorization logic; only LaverSneglen's physical traversal was observed. Offline tests do not replace these distinctions.

Delivery records:

- All 174 repository script mirrors match the final normalized Studio export; zero editor conflicts. All 12 Level 4 sources are unchanged. Class, instance path, source hash and attributes are in `artifacts/level5-20260923/studio-source-verification.json`.
- Native post-change backup saved and binary header verified: `output/level5-build/Backrooms-Level5-v2026.rbxl` (9,462,909 bytes; SHA-256 `37629bcccb5af38c150e2c7015f318942f0d1d1b0f761cff125309b6b7e221ab`). Both native backups are recorded in `artifacts/level5-20260923/native-backups.json`.
- Delivery branch: `codex/level5-indoor-suburbs`. Source mirrors, textures, screenshots, tests and evidence are included; native backup files remain local. Trello records the final commit/PR links.
- Roblox publication is complete; preserve its receipt separately from the subsequent Git commit and push records.
- Trello map-delivery checklist uses the actual test scope above. Deferred entity/puzzle/slide/completion tasks stay incomplete.

Roblox publication v2026 is confirmed. Final GitHub commit/push links are recorded on the Trello delivery card.
