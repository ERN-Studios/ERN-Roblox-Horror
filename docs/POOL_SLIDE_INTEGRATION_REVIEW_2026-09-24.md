# Pool Slide integration review — 24 September 2026

Independent read-only review of Claude commit `234c66e` for [Pool Slide Trello](https://trello.com/c/rXhi1SZ8). The larger entity is genuinely in Studio Edit: the original 20-bone rig was scaled to **1.10×** its verified scale-6 template, with Walk Animation `103719823156557` and Run Animation `128800704640816`. The old template remains as an in-Studio backup sibling. The uploaded Model `95190427565492` is **not** the integrated rig: importing it lost Head and Neck, leaving only 18 bones. A 1.20× live-template scale caused long enraged plans to expire; 1.10× passed Claude's four reported rounds. Its template attributes are AgentRadius 8.25, AgentHeight 18.37, GroundOffset 6.6, animation reference speeds 13.62/33.74 and AttackDistance 11.55.

Fresh Studio Source and repository code for Rig Adapter and Controller match. Both Luau files compile. Offline release (372), spawn separation (8) and audio state (44) checks pass. The navigation test's current-source checks pass (7). Its old negative-control fixture read current `HEAD` and failed after the fix was committed; Codex pinned it to the verified historical stalled source at `715e4d9`. The full navigation suite now passes, including two expected failures against that pre-fix source. The commit message reports four active Level 2 rounds and per-seed behavior, but I found no independently stored per-seed traces. A later read-only Open Cloud download preserved the integrated template in a [full native saved-place backup](../artifacts/poolslide-20260924/README.md) at v2074, with an offline-verified 20-bone rig and its animation IDs; this is a saved revision, not a publication receipt.

**Keep the card To Do.** Verify animation foot contact/stride and the attack envelope in a recorded live round at walk 10, chase 20 and enraged 32; Claude noted foot sliding and a Walk foot-bone dip of about 0.15 studs below rest. The [isolated Studio A/B](POOL_SLIDE_AB_2026-09-24.md) now confirms 31/64 Walk and 19/39 Run all-bone rest flashes in v1, versus zero in both uploaded v2 candidates. The scale-neutral pair measures smoother and with less foot-bone dip on a test copy, but the active rig still uses v1. Measure FPS/memory on physical phone and tablet, then publish after verification and record the place version/receipt. Published v2061 predates `234c66e`.
