# RoundUI controller-switch regression — 24 September 2026

Read-only review of Claude commit `79b42d8` against [the open controller card](https://trello.com/c/uI8hg2At). Eight focused Luau suites pass: 14 teleport, 86 loading-host, 61 entry, 622 wheel, 505 Daily, 306 HUD, 31 RoundUI focus and 8 Pool Slide checks. Published multiplayer and physical-controller acceptance still remain.

One additional controller gap is reproducible. `StarterPlayer/StarterPlayerScripts/RoundUI.LocalScript.lua` assigns `GuiService.SelectedObject` for queue/completion only when those screens **open** while `UIDevice.LastInput()` is Gamepad (around lines 874 and 1520–1522). RoundUI has no `LastInputTypeChanged` focus path. Open the queue or completion screen with keyboard/mouse, then switch to a controller while the screen stays visible: selection stays `nil`, so directional navigation/A has no target. A temporary Luau 0.737 harness reused the real queue/completion blocks from `test_roundui_controller_focus.py` and added switch-while-open assertions; both observed `SelectedObject == nil` (33 checks total). No repository or Studio code was changed for this review.

Fix contract: on transition to Gamepad, while Roblox menu is closed, set focus inside the currently visible queue or completion modal if no valid control there is already selected. Pick the enabled queue submit button, or an active CONTINUE/BACK TO LOBBY choice as appropriate. Preserve an existing valid focus in that modal; release only focus owned by RoundUI on close. Add switch-while-open regression cases to `test_roundui_controller_focus.py`. Then run the actual physical controller/kiosk/settings/disconnect cases in [the consolidated QA sheet](PHYSICAL_QA_2026-09-24.md) before Done.

Claude commit `d50033f` added a `LastInputTypeChanged` handler for already-open queue/completion modals. The expanded RoundUI focus suite passes 39 checks against the new code. This closes the deterministic source repro; actual Roblox focus behavior with a physical controller, including kiosk/settings/B/disconnect, remains unverified and the Trello card stays Testing. The commit is newer than the v2061 publication receipt.
